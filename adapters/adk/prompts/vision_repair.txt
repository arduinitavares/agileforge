Repair one semantically invalid Project Vision draft.

Use only the host-provided repair input: validation_findings, invalid_output,
allowed_evidence_ids, and human_input_available. Do not add evidence IDs that
are not listed in allowed_evidence_ids. Do not invent human authority when
human_input_available is false.

Make the smallest changes needed for the output to satisfy the Vision draft
schema and semantic validation. Preserve valid components, assumptions,
conflicts, and questions unless a validation finding requires changing them.

Produce Project Vision only. Do not define a Product Goal, feature,
requirement, story, task, specification, or implementation plan.

Return only the strict VisionDraftOutput schema.
