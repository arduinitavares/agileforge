# Historical schema evidence

Everything below this directory is historical evidence. It is not current
schema or runtime guidance and must not be imported or copied into production.

The current runtime uses a fresh-schema hard break and the exact human-accepted
`agileforge.spec.v2` Specification as its delivery contract.
